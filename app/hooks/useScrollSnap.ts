'use client';

import { useEffect } from 'react';

export function useScrollSnap(options = {}) {
  // Temporarily disabled to fix build issues
  useEffect(() => {
    // Hook functionality disabled
    return () => {};
  }, []);
} 